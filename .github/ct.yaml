# Chart version is owned by release-please based on conventional commits;
# requiring a manual Chart.yaml version bump per PR would conflict with
# that flow.
helm-extra-args: --timeout 600s
check-version-increment: false
debug: true